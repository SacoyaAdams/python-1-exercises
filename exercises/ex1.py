def hello_world(num):
    #create a function that accepts a string
    #convert the string to an integer
    #use a for loop to print the message to the console
    x = int(num)
    # create the for loop for how many times it will repeat
    for i in range(x):
         print("Hello_world from Python!")
